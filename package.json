{
  "name": "tpc-polis",
  "version": "0.0.1",
  "description": "A node package created for the SGR SSR hackday as a client to access the tpc Polis API.  The aim was to practise using JavaScript and Promises, and create an npm package for the first time.",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NJ Pearman",
  "license": "ISC",
  "dependencies": {
    "buffer": "^5.1.0",
    "dotenv": "^5.0.1",
    "npm": "^5.7.1",
    "request": "^2.83.0"
  }
}
